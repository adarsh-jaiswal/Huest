<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.example.amankumar.huest.Login.GuestOrHuestActivity">

    <include
        android:id="@+id/app_bar"
        layout="@layout/appbar" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="@dimen/marginLeft"
        android:layout_marginRight="@dimen/marginRight"
        android:orientation="vertical">

        <Button
            android:id="@+id/SignUpAsGuestButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="70dp"
            android:background="@color/primaryColor"
            android:elevation="4dp"
            android:onClick="signUpAsGuestButton"
            android:text="GUEST"
            android:textColor="@color/colorText" />
        <Button
            android:id="@+id/SignUpAsHostButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="40dp"
            android:background="@color/primaryColor"
            android:elevation="4dp"
            android:onClick="signUpAsHostButton"
            android:text="HOST"
            android:textColor="@color/colorText" />
    </LinearLayout>

</LinearLayout>
