<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amankumar.huest" >

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/app"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".UI.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.GuestSignUpActivity"
            android:label="@string/title_activity_sinup"
            android:parentActivityName=".UI.GuestOrHostActivity" >
        </activity>
        <activity
            android:name=".UI.GuestOrHostActivity"
            android:label="@string/title_activity_guest_or_host"
            android:parentActivityName=".UI.MainActivity" >
        </activity>
        <activity
            android:name=".UI.GuestActivity"
            android:label="@string/title_activity_guest" >
        </activity>
        <activity
            android:name=".UI.HostSignUpActivity"
            android:label="@string/title_activity_host_sign_up" >
        </activity>
        <activity
            android:name=".UI.HostActivity"
            android:label="@string/title_activity_host" >
        </activity>
    </application>

</manifest>
